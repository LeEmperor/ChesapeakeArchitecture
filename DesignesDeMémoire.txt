Intention:
    construire un unit de mémoire qu'a la capacité de garder les outputs d'un compsant jusqu'à un ENVOYER signale est envoie par un controller (via un bus de controller)

l'état defaulte doit être plus-haût d'impedance -> ni L ou H

1: 
    le siganle d'envoyer N'NEST PAS envoie
    - c'est importante que le unit regarde le data 
    - et que le data est regardé jusqu'à un RESET signal (n'est pas après un ENVOYER signal)
    - la capacité de beacoup des signals d'ENVOYER

    - la capacité pour un single-envoyer mémoire que fait le RESET après un envoyer utilisation


Avec Buffers:
    Behavioure Intendé:
        Garder le resulte previeuse
        Quand c'est llamar, passer l'information en mémoire au controller (via les lignes de data )








